<div id="generic-exposures-list" class='generic-exposures-list-container'>
  <div class='text-center input-page-heading'>
    <h2>Generic Exposures</h2>
  </div>
  <table id='generic-exposures-table' class='table table-striped'>
    <thead>
      <tr>
        {{!-- <th id='starred-heading'>Starred</th> --}}
        <th id='exposures-id-heading' class='table-heading' {{action 'sortTableById'}}>ID</th>
        <th id='generic-activity-heading' class='table-heading' {{action 'sortTableByActivity'}}>Activity</th>
        <th id='generic-fear-heading' class='table-heading' {{action 'sortTableByFearLevel'}}>Fear Level</th>
        <th id='generic-delete-heading' class='table-heading'>Delete</th>
        <span class="table-add glyphicon glyphicon-plus" {{action 'createGenericsEntry'}}></span>
      </tr>
    </thead>
    <tbody id='generic-exposures-table-body'>
    {{#each genericsEntries as |generic|}}
      <tr class="file-row" data-id="{{generic.id}}">
      {{!-- <td>{{journalEntry.starred}}</td> --}}
      <td class='text-center'>{{generic.id}}</td>
      <td class='text-center'>{{input value=generic.activity}}</td>
      <td class='text-center'>{{input type="number" value=generic.fear_level}}</td>
      {{!-- <td><button class='btn-info' {{action "updateEntry"}}>Update</button></td> --}}
      <td class='text-center'><button class='btn-danger'{{action "deleteEntry"}}>Delete</button></td>
      </tr>
    {{/each}}
  </tbody>
  </table>
  <div id='save-generics-button-wrapper' class='text-center'>
    <button class='btn btn-info' {{action 'submit'}}>Save Table</button>
  </div>
</div>
